#include "main.h"

/**
 * print_percentage - a function that prints the  % character
 * @lists: contains arguments
 * Return: 1
 */
int print_percentage(va_list __attribute__((unused)) lists)
{
	_putchar('%');

	return (1);
}
